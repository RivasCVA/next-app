import Button from '@components/Button';
import { FC } from 'react';

const Home: FC = () => (
    <div className="h-full w-full bg-white">
        <Button>Hello</Button>
    </div>
);

export default Home;
